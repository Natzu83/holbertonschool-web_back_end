#!/usr/bin/env python3
"""
A function named 'to_str' , uses the Python type hints to specify that
'to_str' takes in a 'float' and returns a 'str'.
"""


def to_str(n: float) -> str:
    """
    Assuming that 'n' is a float,
    returns the string representation
    of 'n'.
    """
    return str(n)
